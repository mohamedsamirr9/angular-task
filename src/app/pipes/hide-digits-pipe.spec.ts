import { HideDigitsPipe } from './hide-digits-pipe';

describe('HideDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new HideDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
