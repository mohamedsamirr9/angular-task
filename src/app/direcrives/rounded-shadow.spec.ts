import { RoundedShadow } from './rounded-shadow';

describe('RoundedShadow', () => {
  it('should create an instance', () => {
    const directive = new RoundedShadow();
    expect(directive).toBeTruthy();
  });
});
